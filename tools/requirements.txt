# BLE OTA functionality
bleak>=0.21.0

# Grinder Data Analysis Tool Dependencies  
pyserial>=3.5
pandas>=2.0.0
matplotlib>=3.3.0
scipy>=1.9.0

# Streamlit visualization and reporting
streamlit>=1.28.0
plotly>=5.15.0

# Build system (use PlatformIO from the venv, not global `pio`)
platformio>=6.0.0

# Pinned exactly: patches created here must stay decodable by the C decoder
# baked into shipped firmware (components/detools). Bumping either pin
# requires the host compat test (test_patch_compat) to pass first.
detools==0.53.0
heatshrink2==0.14.0

# Cross-platform color support
colorama>=0.4.0
